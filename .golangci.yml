linters-settings:
    golint:
        min-confidence: 0.1
    goimports:
        local-prefixes: emperror.dev/emperror

linters:
    enable-all: true
    disable:
        - funlen
        - wsl

service:
    golangci-lint-version: 1.21.x
